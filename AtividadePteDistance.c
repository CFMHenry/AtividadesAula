#include<stdio.h>
#include<stdlib.h>

void CalcDistancia(){
    int inicioStr = 7;
    char pontos[100];
    char Linha[256];

    scanf("%s", Linha);

    for(;Linha[inicioStr] != '\0'; inicioStr++){
        if(Linha)
    }
}

int main(){
    int LinhasLidas;
    

    scanf("%d", &LinhasLidas);
    for(int i = 0; i < LinhasLidas; i++){
        CalcDistancia();
    }
    return 0;
}